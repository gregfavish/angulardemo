Additional Info
4.	Create docker images for the different elements that make up the site (e.g. one for the website, another for the database etc.). 
	There should be a docker.compose.yml file in the project which builds all the images and brings up the working website.
	
Improvemtns - improve environemntal configuration 
			- Authentication works but could be better integrated between server and client
			-centralize api url
			-error handling on client side when things are slow or error on the API
	
things to install : sql server local 
	
Angular

cd C:/dev/AbsaProj/Angular/absaproj
docker build -t angular-client:dev .
docker run -d --name angular-client -p 4200:4200 angular-client:dev

.net API
problem -- cant start windows container
https://docs.microsoft.com/en-us/virtualization/windowscontainers/deploy-containers/system-requirements
cd C:/dev/AbsaProj
docker build -t dotnet-api:dev .
docker run -d --name dotnet-api -p 4300:4300 dotnet-api:dev


database:
cd C:/dev/AbsaProj/Database
docker build -t database-sql:dev .
docker run -e 'ACCEPT_EULA=Y' -e 'SA_PASSWORD=yourStrong(!)Password' -p 1433:1433 -d database-sql:dev
docker exec -it 4e /opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P 'yourStrong(!)Password'

